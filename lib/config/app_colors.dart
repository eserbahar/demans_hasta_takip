import 'package:flutter/material.dart';

/// Approved design-preview palette (see implementation plan, "Visual Design
/// Direction"). Keep in sync with [activity_categories.dart] per-category
/// accents.
class AppColors {
  AppColors._();

  static const background = Color(0xFFF6F2EA);
  static const primary = Color(0xFF2F6F5E);
  static const bodyText = Color(0xFF2B2A28);
  static const mutedText = Color(0xFF8A8378);
  static const cardBorder = Color(0xFFECE6D8);
}
